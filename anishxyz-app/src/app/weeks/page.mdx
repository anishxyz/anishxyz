import LifeWeeks from "@/components/LifeWeeks"
import { buildLifeWeeks } from "@/lib/life-weeks"
import { loadLifeWeeksConfig } from "@/lib/life-weeks-config"

export const metadata = {
  title: "Weeks"
}

# Weeks
my life in weeks
{/*
  Build the data on the server and pass to the client component for rendering.
*/}
<LifeWeeks
  data={buildLifeWeeks(loadLifeWeeksConfig())}
  todayISO={new Date().toISOString().slice(0, 10)}
/>
